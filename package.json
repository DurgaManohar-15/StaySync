{
  "name": "stay-sync-fullstack",
  "version": "1.0.0",
  "description": "Stay Sync - Student Hostel & PG Finder System",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "server": "node server.js",
    "client": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "init-db": "node init-db.js",
    "build-client": "npm run build --prefix frontend"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-session": "^1.18.1",
    "mysql": "^2.18.1",
    "nodemailer": "^9.0.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
